# Build output
/build/
/dist/
/dist.zip
*.spec

# CLIP model weights (regenerated by download_model.py)
/model_cache/

# Python
__pycache__/
*.pyc

# Local indexing caches (created inside whatever folder the user indexes)
.imagesearch_cache.json
.imagesearch_sp_index.json

# Editor / tool local state
.claude/
